/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Full-size test profile — a realistic end-to-end dataset.
    Placeholder until a public dRNA-seq test dataset is wired in (Phase 1+).
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

params {
    config_profile_name        = 'Full test profile'
    config_profile_description = 'Full-size dRNA-seq test dataset (TBD)'

    // Will point at a remote, publicly hosted dataset once selected.
    input = "${projectDir}/assets/samplesheet_test.csv"
    mode  = 'reference'
}
